# GitHub 自动生成 Release 日志时的分组规则。
#
# 只认 label 与作者，**没有**约定式提交分类器——`labels` 里的 `*` 只是「兜底」，
# 不是通配符（GitHub 文档：Use `*` as a catch-all），所以这里必须是**精确**的标签名。
# 标签由 .github/workflows/autolabeler.yml 按 PR 标题（约定式提交）自动补上，不需要手动打。
#
# 分类是**顺序**匹配、第一个命中即止，因此兜底的「其它改动」必须放最后。
# 带 ! 的破坏性提交会同时命中 breaking-change 与 enhancement/bug，靠顺序让它落进第一节。
changelog:
  categories:
    - title: 💥 破坏性变更
      labels:
        - breaking-change
    - title: ⬆️ 依赖更新
      labels:
        - dependencies
    - title: ✨ 新功能
      labels:
        - enhancement
    - title: 🐛 修复
      labels:
        - bug
    - title: 📖 文档
      labels:
        - documentation
    - title: 🧰 维护
      labels:
        - chore
    # 兜底：收下前面都没命中的 PR。
    - title: 🧰 其它改动
      labels:
        - '*'
