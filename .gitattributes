# 全仓库一律 LF：仓库里、检出后都是 LF。
#
# 这台机器的 core.autocrlf 是 scoop 的 system gitconfig 打开的（= true），而仓库里的属性文件
# 优先级高于它——写在这里，别的 clone、别的机器、CI 都不必各自去关一遍。顺带解决两个具体麻烦：
# pnpm-lock.yaml / pnpm-workspace.yaml 这类工具生成的文件不会在检出时变成 CRLF，也就不会出现
# 「LF will be replaced by CRLF」之后再看到整文件 diff。git 的 text=auto 会让它自己判断二进制，
# 因此 test/fixtures/*.json 这类数据文件也不会被误伤。
* text=auto eol=lf
